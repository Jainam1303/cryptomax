# MongoDB Connection
# For local development (recommended for testing)
MONGO_URI=mongodb://localhost:27017/crypto-investment-platform

# Or use MongoDB Atlas (replace with your actual connection string)
# MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/crypto-investment-platform?retryWrites=true&w=majority

# For testing without database (uses mock data only)
# MOCK_MODE=true

# Server Port (optional, defaults to 5000)
PORT=5000

# JWT Secret for authentication (use a secure random string in production)
JWT_SECRET=your-super-secure-long-random-string-here-make-it-long-and-random

# JWT Token Expiration (optional, defaults to 24h)
JWT_EXPIRATION=24h

# Node Environment
NODE_ENV=development
